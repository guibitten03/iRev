# iRev
A framework of Review Aware Recommender Systems

## How to run the example

```shell
$ python3 irev.py execute-experiment --algorithms RMG --datasets "Amazon Fashion" --metrics Hits --extra-modules example.RMG --config-file example/Config.yaml
```